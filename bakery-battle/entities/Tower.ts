import * as THREE from "three"; export class Tower extends THREE.Group { constructor(x, z, type) { super(); this.position.set(x, 0, z); const color = type === "bakery" ? 0xffd700 : 0x00d4ff; const head = new THREE.Mesh(new THREE.TorusKnotGeometry(0.2, 0.05, 64, 8), new THREE.MeshStandardMaterial({ color, metalness: 0.8, roughness: 0.2 })); head.position.y = 0.5; this.add(new THREE.Mesh(new THREE.CylinderGeometry(0.4, 0.45, 0.2, 12), new THREE.MeshStandardMaterial({ color: 0x1a2639 })), head); } }
