{
  "name": "phbakergame",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:game": "npm --workspace apps/game run dev",
    "dev:editor": "npm --workspace apps/editor run dev",
    "build": "npm --workspace apps/game run build && npm --workspace apps/editor run build",
    "lint": "echo \"lint: add eslint later\"",
    "validate:assets": "node scripts/validate-assets.mjs"
  }
}
