import * as THREE from "three"; export class Tower extends THREE.Group { constructor(x, z, theme) { super(); this.position.set(x, 0, z); const color = parseInt(theme.primary); const headGeo = theme.tower === "Mixer" ? new THREE.TorusKnotGeometry(0.18, 0.06, 64, 8) : new THREE.ConeGeometry(0.2, 0.6, 4); const head = new THREE.Mesh(headGeo, new THREE.MeshStandardMaterial({ color, metalness: 0.9, roughness: 0.1 })); head.position.y = 0.6; this.add(new THREE.Mesh(new THREE.CylinderGeometry(0.4, 0.45, 0.2, 12), new THREE.MeshStandardMaterial({ color: 0x1a2639 })), head); } }
