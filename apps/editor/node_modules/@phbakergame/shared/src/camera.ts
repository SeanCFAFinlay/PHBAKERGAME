export type GameplayCameraConfig = {
  fov: number;
  near: number;
  far: number;
  position: [number, number, number];
  lookAt: [number, number, number];
};

export const GAMEPLAY_CAMERA: GameplayCameraConfig = {
  fov: 50,
  near: 0.1,
  far: 500,
  position: [0, 14, 18],
  lookAt: [0, 0, 0]
};

